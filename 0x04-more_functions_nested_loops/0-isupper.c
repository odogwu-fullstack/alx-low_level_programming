#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 1 or 0 (Success)
*/
int _isupper(int c)
{
	if (c > 65)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
